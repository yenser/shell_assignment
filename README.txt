To Compile:

run command 'make' to compile all the code.



To Run:

run command './main' to run the shell.



Known Issues:

Piping doesn't work. Code was commented out so that it functions like the ; command. Due to that,
we also don't have && working and don't know if parenthesis works.
